# WikiFlow 环境变量配置模板
# 复制此文件为 .env 并填入你的实际路径

# 单仓库模式：代码在同一目录下
# WIKIFLOW_CODE_ROOT=src

# 多仓库模式：文档代码分离，每个仓库独立路径
# 示例：后端代码在 ../my-project-backend/src
# WIKIFLOW_BACKEND_ROOT=../my-project-backend/src
#
# 示例：前端代码在 ../my-project-frontend/src
# WIKIFLOW_FRONTEND_ROOT=../my-project-frontend/src

# 注意事项：
# 1. 环境变量中的路径可以是相对路径或绝对路径
# 2. 相对路径是相对于文档仓库的根目录
# 3. .env 文件已加入 .gitignore，不会提交到仓库
# 4. 每个团队成员可以有不同的路径配置
